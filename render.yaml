# render.yaml — Configuration Render.com
services:
  - type: web
    name: ia-telecom-chatbot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app_flask:app --bind 0.0.0.0:$PORT --timeout 120
    envVars:
      - key: GEMINI_API_KEY
        sync: false
      - key: PYTHON_VERSION
        value: 3.10.12
